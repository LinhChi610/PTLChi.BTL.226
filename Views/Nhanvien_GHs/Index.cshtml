@model IEnumerable<PTLChi.BTL._226.Models.Nhanvien_GH>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Thông Tin Nhân Viên Giao Hàng</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ten_NhanvienGH)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sdt_1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sdt_2)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ten_NhanvienGH)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sdt_1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sdt_2)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.ID_NhanvienGH }) |
            @Html.ActionLink("Thông Tin Chi Tiết", "Details", new { id=item.ID_NhanvienGH }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.ID_NhanvienGH })
        </td>
    </tr>
}

</table>
