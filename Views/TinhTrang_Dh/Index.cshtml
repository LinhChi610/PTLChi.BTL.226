@model IEnumerable<PTLChi.BTL._226.Models.TinhTrang_dh>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TinhTrangdh)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TinhTrangdh)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_tinh_trang }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_tinh_trang }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_tinh_trang })
        </td>
    </tr>
}

</table>
