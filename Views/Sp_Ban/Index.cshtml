@model IEnumerable<QLBHDTDD.Models.Sp_Ban>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SanPhams.ID_Danhmuc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.So_luong_ban)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SanPhams.ID_Danhmuc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.So_luong_ban)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Sp_Ban }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Sp_Ban }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Sp_Ban })
        </td>
    </tr>
}

</table>
