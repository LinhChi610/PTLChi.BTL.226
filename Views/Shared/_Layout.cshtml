<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width-device-width, initial-scale=2.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active mx-2">@Html.ActionLink("Danh mục sản phẩm", "Index", "Danhmuc_sp")</li>
                <li class="nav-item active mx-2">@Html.ActionLink("Sản Phẩm", "Index", "SanPhams")</li>
                <li class="nav-item active mx-2">@Html.ActionLink("Khách Hàng", "Index", "Khach_Hang")</li>
                <li class="nav-item active mx-2">@Html.ActionLink("Đơn Đặt Hàng", "Index", "Don_dh")</li>
                <li class="nav-item active mx-2">@Html.ActionLink("Chi tiết đơn hàng", "Index", "Chitiet_dh")</li>
                <li class="nav-item active mx-2">@Html.ActionLink("Nhân viên giao hàng", "Index", "Nhanvien_GH")</li>

                <li>
                    @if (!User.Identity.IsAuthenticated)
                    {
                        @Html.ActionLink("Đăng Nhập", "Login", "AccountModels")
                    }
                    else
                    {
                        @Html.ActionLink("Đăng xuất", "Logout", "AccountModels")
                    }
                </li>
            </ul>
        </div>
     </nav>   
        <div class="container body-content">
            @RenderBody()
            <hr />
            <footer>
                <p>&copy; @DateTime.Now.Year - BTL.LTQL.Phạm Thị Linh Chi</p>
            </footer>
        </div>

        @Scripts.Render("~/bundles/jquery")
        @Scripts.Render("~/bundles/bootstrap")
        @RenderSection("scripts", required: false)
</body>
</html>
